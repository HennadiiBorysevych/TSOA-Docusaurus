{
  "name": "do-toap",
  "version": "1.0.0",
  "description": "Track 2 — TSOA + Scalar proof of concept",
  "scripts": {
    "dev": "tsoa spec-and-routes && tsx watch src/server.ts",
    "docs:generate": "tsoa spec",
    "build": "tsoa spec-and-routes && tsc"
  },
  "dependencies": {
    "@scalar/express-api-reference": "^0.4.177",
    "express": "^5.0.0",
    "tsoa": "^6.0.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
